MISTLIB_REPO=git@github.com:<org>/<repo>.git
MISTLIB_REF=develop

# Both variables above can also be set as real environment variables, which
# take precedence over this file. Used by `just vendor-mistlib-check` (drift
# detection) and `just vendor-mistlib-update` (detect, re-vendor, and commit).
